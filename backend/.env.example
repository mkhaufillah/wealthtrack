# WealthTrack Backend — Environment Variables
# Copy to .env and fill in real values before running.

SECRET_KEY=change-me-to-a-random-secret-key
DEBUG=True
ACCESS_TOKEN_EXPIRE_DAYS=30
# JSON array — override for production (e.g. Flutter domain)
# CORS_ORIGINS=["https://wealthtrack.filla.id"]

# AI API Keys
OPENCODE_GO_API_KEY=
OPENROUTER_API_KEY=
BRAVE_SEARCH_API_KEY=
