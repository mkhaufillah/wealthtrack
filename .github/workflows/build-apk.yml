name: Build Flutter APK

on:
  push:
    branches: [main]
    paths:
      - 'mobile/**'
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  TG_CHAT_ID: "-1003997003114"
  TG_TOPIC_ID: "3"

permissions:
  contents: write
  actions: read
  packages: read

concurrency:
  group: build-apk-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: [self-hosted, linux]
    defaults:
      run:
        working-directory: mobile

    steps:
      - uses: actions/checkout@v6

      - name: 🚀 Notify — APK build started
        run: |
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
            -d chat_id="${{ env.TG_CHAT_ID }}" \
            -d message_thread_id="${{ env.TG_TOPIC_ID }}" \
            -d parse_mode="HTML" \
            -d text="<b>📱 WealthTrack APK</b>%0A<code>${GITHUB_SHA::7}</code> — building... ⏳"

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      - name: Create platform directories
        run: flutter create --project-name wealthtrack --org com.filla --platforms android .

      - name: Setup release signing
        env:
          KEYSTORE_BASE64: ${{ secrets.KEYSTORE_BASE64 }}
          KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
        run: python3 ../scripts/ci_release_setup.py

      - name: Fix app display name (wealthtrack → WealthTrack)
        run: |
          sed -i 's/android:label="wealthtrack"/android:label="WealthTrack"/g' android/app/src/main/AndroidManifest.xml

      - name: Patch AndroidManifest for network permissions
        run: python3 ../scripts/ci_patch_manifest.py

      - name: Ensure ProGuard rules for release build
        run: |
          cat > android/app/proguard-rules.pro << 'EOF'
          # Flutter/Dart HTTP client connectivity
          -keep class io.flutter.** { *; }
          -dontwarn io.flutter.**

          # OkHttp (used by Flutter on Android for network)
          -keep class okhttp3.** { *; }
          -keep interface okhttp3.** { *; }
          -dontwarn okhttp3.**
          -dontwarn okio.**

          # Keep all model/serialization classes
          -keep class com.filla.wealthtrack.** { *; }
          EOF

      - name: Verify AndroidManifest has INTERNET permission
        run: |
          grep -q "INTERNET" android/app/src/main/AndroidManifest.xml && echo "✅ INTERNET permission found" || echo "❌ INTERNET permission MISSING"
          echo "--- Manifest content ---"
          cat android/app/src/main/AndroidManifest.xml

      - name: Cache pip packages
        uses: actions/cache@v5
        with:
          path: ~/.cache/pip
          key: pip-${{ runner.os }}-pillow
          restore-keys: |
            pip-${{ runner.os }}-

      - name: Generate app icons
        run: |
          pip install --break-system-packages Pillow
          python3 generate_icons.py --platforms android

      - name: Install dependencies
        run: flutter pub get

      - name: Remove default test file
        run: rm -f test/widget_test.dart

      - name: Run tests
        run: flutter test

      - name: Build APK (release)
        run: flutter build apk --release

      - name: Verify release APK
        run: |
          APK_PATH="build/app/outputs/flutter-apk/app-release.apk"
          if [ -f "$APK_PATH" ]; then
            SIZE=$(stat -c%s "$APK_PATH")
            echo "✅ APK found: $APK_PATH (${SIZE} bytes)"
            if [ "$SIZE" -gt 1048576 ]; then
              echo "✅ APK size > 1MB (${SIZE} bytes)"
            else
              echo "❌ APK size is only ${SIZE} bytes (expected > 1MB)"
              exit 1
            fi
          else
            echo "❌ APK not found at $APK_PATH"
            exit 1
          fi

      - name: Upload release APK
        id: upload-artifact
        continue-on-error: true
        uses: actions/upload-artifact@v7
        with:
          name: wealthtrack-release
          path: mobile/build/app/outputs/flutter-apk/app-release.apk
          retention-days: 1

      - name: Upload APK to GitHub Release (fallback — quota habis)
        if: steps.upload-artifact.outcome == 'failure'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          APK_PATH="build/app/outputs/flutter-apk/app-release.apk"
          TAG="ci-build-${GITHUB_SHA::7}"
          gh release create "$TAG" "$APK_PATH" \
            --title "CI Build ${GITHUB_SHA::7}" \
            --notes "Auto-built from commit ${GITHUB_SHA}" \
            --target "$GITHUB_SHA" \
            --latest=false \
            --prerelease || \
          gh release upload "$TAG" "$APK_PATH" --clobber || true

      - name: Telegram notification — success
        if: success()
        run: |
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
            -d chat_id="${{ env.TG_CHAT_ID }}" \
            -d message_thread_id="${{ env.TG_TOPIC_ID }}" \
            -d parse_mode="HTML" \
            -d text="<b>✅ WealthTrack APK</b>%0ABuild APK <code>${GITHUB_SHA::7}</code> berhasil 🚀%0ARun: <a href=\"https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}\">#${{ github.run_id }}</a>"

      - name: Telegram notification — failure
        if: failure()
        run: |
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
            -d chat_id="${{ env.TG_CHAT_ID }}" \
            -d message_thread_id="${{ env.TG_TOPIC_ID }}" \
            -d parse_mode="HTML" \
            -d text="<b>❌ WealthTrack APK</b>%0ABuild APK <code>${GITHUB_SHA::7}</code> gagal!%0A<a href=\"https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}\">Lihat logs</a>"

      - name: 🧹 Cleanup build artifacts
        if: always()
        run: |
          echo "Cleaning up APK build artifacts..."
          rm -rf build/
          rm -rf .dart_tool/
          rm -rf .packages .flutter-plugins .flutter-plugins-dependencies
          echo "✅ Cleanup done — $(df -h . | tail -1 | awk '{print $4}') free"
