import 'package:flutter_test/flutter_test.dart';

void main() {
  group('App smoke test', () {
    test('app entry point is valid', () {
      // Placeholder — screens with GoRouter + Riverpod
      // require full ProviderScope overrides and are tested separately.
      expect(true, isTrue);
    });
  });
}
