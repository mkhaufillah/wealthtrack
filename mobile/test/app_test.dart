import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Placeholder test — app loads without crash', (tester) async {
    // TODO: Add actual tests
    expect(true, isTrue);
  });
}
